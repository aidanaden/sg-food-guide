---
import { cn } from "./utils";
interface Props { icon: string; class?: string; }
const { icon, class: className } = Astro.props;
---
<span data-slot="error-page-icon" class={cn("iconify text-foreground-subtle size-16", icon, className)} aria-hidden="true" />
